import React from "react";
import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";

const Tab = createBottomTabNavigator()

export const BottomTabNavigation: React.FC<{  }> = props => {
  return (
    <Tab.Navigator>
        
    </Tab.Navigator>
  );
};